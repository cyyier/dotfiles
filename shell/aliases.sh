# safer defaults
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

# ls family
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# git shortcuts
alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias gco='git checkout'
alias gl='git log --oneline --graph --decorate'
alias gb='git branch'

# navigation
alias ..='cd ..'
alias ...='cd ../..'
alias -='-'

# misc
alias cls='clear'

# other
alias mkdir='mkdir -p'
alias h='history'
alias j='jobs'