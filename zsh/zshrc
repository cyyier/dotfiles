
# only interactive
[[ -o interactive ]] || return

# Path to installation.
export PATH="$HOME/.local/bin:$PATH"
export ZSH="$HOME/.oh-my-zsh"


ZSH_THEME=""

plugins=(git zsh-autosuggestions zsh-syntax-highlighting)

source $ZSH/oh-my-zsh.sh

source ~/dotfiles/shell/aliases.sh

ECHO_PINK="\e[38;5;218m"
ECHO_RESET="\e[0m"

# Read custom environment name if it exists
if [[ -f "$HOME/.env_name_tag" ]]; then
    export ENV_TYPE=$(cat "$HOME/.env_name_tag")
else
    export ENV_TYPE="Default"
fi



print
print "   ✧( •̀ ω •́ ) Happy coding  ( •̀ ω •́ )✧" 
print "───────────────────────────────────────────"
print "${ECHO_PINK}Welcome back to your $ENV_TYPE, soso 🌸${ECHO_RESET}"
print

eval "$(starship init zsh)"


# # only interactive
# [[ -o interactive ]] || return

# source ~/dotfiles/shell/aliases.sh
# source ~/dotfiles/shell/prompt.sh

# autoload -U colors && colors

# PINK="%F{218}"
# BLUE="%F{110}"
# GRAY="%F{245}"
# PURPLE="%F{177}"

# RESET="%f"

# ECHO_PINK="\e[38;5;218m"
# ECHO_RESET="\e[0m"

# print
# print "  ✧( •̀ ω •́ )      ( •̀ ω •́ )✧"
# print "  ───────────────────────────"
# print "     ${ECHO_PINK}welcome back, soso 🌸${ECHO_RESET}"
# print

# set_prompt() {
#   local p_pink="%{$PINK%}"
#   local p_blue="%{$BLUE%}"
#   local p_gray="%{$GRAY%}"
#   local p_purple="%{$PURPLE%}"
#   local p_reset="%{$RESET%}"

#   PROMPT="${p_pink}💗 ${p_blue}%n@%m ${p_gray}%~ ${branch_info}${p_reset}
# ${p_pink}❯${p_purple}❯${p_blue}❯${p_reset} "
# }

# precmd() {
#   set_prompt
# }

# To customize prompt, run `p10k configure` or edit ~/dotfiles/zsh/p10k.zsh.

